% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{compute_pgee_variances}
\alias{compute_pgee_variances}
\title{Compute PGEE variance estimators}
\usage{
compute_pgee_variances(fit, estimators = "all")
}
\arguments{
\item{fit}{A fitted object returned by \code{\link[=pgee_fit]{pgee_fit()}} or \code{\link[=pgee]{pgee()}}.}

\item{estimators}{Either \code{"all"} for the full package set, or a character
vector of estimator names returned by \code{\link[=available_estimator_names]{available_estimator_names()}}.
Legacy \verb{V_*} aliases are accepted and normalized internally.}
}
\value{
A named list of covariance matrices. Each element is a \verb{p x p} matrix,
where \code{p} is the number of regression parameters in \code{fit}. For pooling
estimators applied to unbalanced data, the returned matrix is still \verb{p x p}
but is filled with \code{NA} values because the direct implementation is not
computable in that setting.
}
\description{
\code{compute_pgee_variances()} computes one or more covariance-matrix estimators
from a fitted PGEE object produced by \code{\link[=pgee_fit]{pgee_fit()}} or \code{\link[=pgee]{pgee()}}. Use
\code{estimators = "all"} to compute the full 14-estimator package set, or pass a
character vector of estimator names to request only the methods you want.
}
\details{
This is currently an advanced API. The function returns the raw covariance
matrices rather than a formatted model summary.

The output order matches the requested order in \code{estimators}.

Estimators that require balanced cluster sizes are still listed in the
returned object for unbalanced designs, but their covariance matrices are
filled with \code{NA} values because the direct implementation is not computable in
that setting.

If you supply an invalid estimator name, the error message points back to
\code{\link[=available_estimator_names]{available_estimator_names()}} and \code{\link[=available_estimators]{available_estimators()}} so you can inspect
the valid choices.
}
\examples{
set.seed(1)
sim <- dgp_generate(
  N = 12,
  gamma = 0.5,
  n_i = 4,
  beta = c(-1.2, 0.6, 0.3),
  rho = 0.2
)

fit <- pgee_fit(
  y = sim$data$y,
  x = sim$data[c("X1", "obstime")],
  id = sim$data$id,
  corr_type = sim$corr_type
)

vars <- compute_pgee_variances(fit, c("MD", "AR"))
names(vars)
dim(vars$AR)
length(compute_pgee_variances(fit))

}
\seealso{
\code{\link[=available_estimators]{available_estimators()}}, \code{\link[=available_estimator_names]{available_estimator_names()}},
\code{\link[=pgee_fit]{pgee_fit()}}
}
