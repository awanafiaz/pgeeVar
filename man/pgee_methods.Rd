% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{pgee_methods}
\alias{pgee_methods}
\alias{print.pgee}
\alias{coef.pgee}
\alias{vcov.pgee}
\alias{summary.pgee}
\alias{print.summary.pgee}
\alias{confint.pgee}
\alias{fitted.pgee}
\alias{residuals.pgee}
\alias{predict.pgee}
\alias{model.frame.pgee}
\alias{model.matrix.pgee}
\alias{formula.pgee}
\alias{nobs.pgee}
\title{Methods for \code{pgee} objects}
\usage{
\method{print}{pgee}(x, ...)

\method{coef}{pgee}(object, ...)

\method{vcov}{pgee}(object, type = NULL, ...)

\method{summary}{pgee}(object, type = NULL, ...)

\method{print}{summary.pgee}(x, ...)

\method{confint}{pgee}(object, parm = NULL, level = 0.95, type = NULL, ...)

\method{fitted}{pgee}(object, ...)

\method{residuals}{pgee}(object, type = c("response", "pearson"), ...)

\method{predict}{pgee}(
  object,
  newdata = NULL,
  type = c("response", "link"),
  se.fit = FALSE,
  vcov_type = NULL,
  ...
)

\method{model.frame}{pgee}(formula, ...)

\method{model.matrix}{pgee}(object, ...)

\method{formula}{pgee}(x, ...)

\method{nobs}{pgee}(object, ...)
}
\arguments{
\item{x}{A \code{"summary.pgee"} object.}

\item{...}{Additional arguments, currently unused.}

\item{object}{A fitted \code{"pgee"} object.}

\item{type}{Variance-estimator name for methods that need a covariance
matrix. If \code{NULL}, the object's stored default variance is used. Canonical
package names are the short forms returned by \code{\link[=available_estimator_names]{available_estimator_names()}};
legacy \verb{V_*} aliases are still accepted.}

\item{parm}{Coefficients to extract in \code{\link[=confint.pgee]{confint.pgee()}}. May be \code{NULL},
character names, logical positions, or numeric indices.}

\item{level}{Confidence level for \code{\link[=confint.pgee]{confint.pgee()}}.}

\item{newdata}{Optional new data for \code{\link[=predict.pgee]{predict.pgee()}}. If \code{NULL}, the method
uses the training data.}

\item{se.fit}{Logical; if \code{TRUE}, \code{\link[=predict.pgee]{predict.pgee()}} also returns standard
errors.}

\item{vcov_type}{Variance-estimator name to use for prediction standard
errors. If \code{NULL}, the object's stored default variance is used. Canonical
package names are the short forms returned by \code{\link[=available_estimator_names]{available_estimator_names()}};
legacy \verb{V_*} aliases are still accepted.}

\item{formula}{A fitted \code{"pgee"} object for \code{\link[=model.frame.pgee]{model.frame.pgee()}}.}
}
\value{
\code{coef.pgee()} returns the named coefficient vector.

\code{vcov.pgee()} returns a single covariance matrix.

\code{summary.pgee()} returns an object of class \code{"summary.pgee"}.

\code{confint.pgee()} returns a two-column confidence-interval matrix.

\code{fitted.pgee()} returns the response-scale fitted values.

\code{residuals.pgee()} returns response or Pearson residuals.

\code{predict.pgee()} returns fitted values, or a list containing fitted
values and standard errors when \code{se.fit = TRUE}.

\code{model.frame.pgee()} returns the stored training model frame.

\code{model.matrix.pgee()} returns the stored training design matrix,
including the intercept column.

\code{formula.pgee()} returns the stored model formula.

\code{nobs.pgee()} returns the total number of observations used in the fit.
}
\description{
These methods provide the first-pass user interface for fitted objects
returned by \code{\link[=pgee]{pgee()}}.
}
