% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{simulate_correlated_binary_data}
\alias{simulate_correlated_binary_data}
\title{Simulate correlated binary longitudinal data}
\usage{
simulate_correlated_binary_data(
  N,
  gamma,
  n_i,
  beta,
  rho,
  corstr = c("exchangeable", "ar1"),
  seed = NULL,
  on_clf_failure = c("warn", "error"),
  output = c("data", "full")
)
}
\arguments{
\item{N}{Number of clusters. Must be an integer greater than or equal to 2.}

\item{gamma}{Proportion of clusters assigned to \code{X1 = 1}. Must lie strictly
between 0 and 1.}

\item{n_i}{Cluster-size specification. Use a single integer for a balanced
design or a length-2 vector \code{c(min, max)} for a discrete uniform range.
Every realized cluster size must be at least 2.}

\item{beta}{Regression coefficients, including the intercept. Supported
lengths are 2, 3, and 4.}

\item{rho}{Correlation parameter used by the CLF generator. Must satisfy
\code{abs(rho) < 1}.}

\item{corstr}{Correlation structure. Must be \code{"exchangeable"} or \code{"ar1"}.}

\item{seed}{Optional integer seed. If \code{NULL}, the current RNG state is left
unchanged. If supplied, the wrapper temporarily sets the seed and restores
the prior RNG state on exit.}

\item{on_clf_failure}{How to handle CLF-generation failures. \code{"warn"} returns
the generated object with a warning; \code{"error"} stops immediately.}

\item{output}{Either \code{"data"} for a plain data frame or \code{"full"} for the
full scenario list.}
}
\value{
If \code{output = "data"}, a data frame with columns \code{id}, \code{y}, and the simulated
covariates, plus a \code{"pgee_scenario"} attribute.

If \code{output = "full"}, a list containing the full low-level scenario object,
with its \verb{$data} component replaced by the stripped user-facing data frame.
}
\description{
\code{simulate_correlated_binary_data()} is the user-facing wrapper around the
lower-level \code{\link[=dgp_generate]{dgp_generate()}} engine. It validates inputs, optionally manages
the RNG seed, and returns either a plain analysis-ready data frame or the
full scenario object.
}
\details{
The \code{"data"} output mode strips the internal intercept column used by the
engine and returns only \code{id}, \code{y}, and the user-facing covariates. The same
stripped frame is stored in \verb{$data} when \code{output = "full"}, so the wrapper's
output intentionally differs from the lower-level \code{\link[=dgp_generate]{dgp_generate()}} engine
object.

When \code{output = "data"}, the returned data frame carries a
\code{"pgee_scenario"} attribute containing the generating metadata, including the
realized event rate.
}
\examples{
sim_data <- simulate_correlated_binary_data(
  N = 12,
  gamma = 0.5,
  n_i = 4,
  beta = c(-1.2, 0.6, 0.3),
  rho = 0.2,
  seed = 1
)

head(sim_data)
attr(sim_data, "pgee_scenario")$clf_ok

sim_full <- simulate_correlated_binary_data(
  N = 12,
  gamma = 0.5,
  n_i = 4,
  beta = c(-1.2, 0.6, 0.3),
  rho = 0.2,
  seed = 1,
  output = "full"
)

sim_full$clf_ok
names(sim_full$data)

}
\seealso{
\code{\link[=pgee]{pgee()}}, \code{\link[=dgp_generate]{dgp_generate()}}, \code{\link[=pgee_fit]{pgee_fit()}}
}
