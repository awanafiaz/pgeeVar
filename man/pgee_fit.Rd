% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pgee_fit.R
\name{pgee_fit}
\alias{pgee_fit}
\title{Fit a penalized generalized estimating equation model}
\usage{
pgee_fit(
  y,
  x,
  id,
  corr_type = c("exchangeable", "ar1", "independence"),
  tol = 1e-04,
  max_iter = 100
)
}
\arguments{
\item{y}{Binary response vector.}

\item{x}{Covariate matrix or data frame \strong{without} an intercept column.}

\item{id}{Cluster identifier vector. Every cluster must contain at least two
observations after missing values are removed.}

\item{corr_type}{Working correlation structure. Must be one of
\code{"exchangeable"}, \code{"ar1"}, or \code{"independence"}.}

\item{tol}{Convergence tolerance for the maximum absolute scoring step.}

\item{max_iter}{Maximum number of scoring iterations.}
}
\value{
A list with fitted values and internal matrices. The most useful
components for end users are:
\itemize{
\item \code{beta}: estimated regression coefficients, including the intercept.
\item \code{converged}: logical convergence flag.
\item \code{iter}: number of scoring iterations used.
\item \code{phi}: estimated dispersion parameter.
\item \code{alpha_hat}: estimated working-correlation parameter. For
\code{corr_type = "independence"}, this value is always returned as \code{0}.
\item \code{N}, \code{p}, \code{n_i}, \code{n_star}: design summaries for the fitted data.
}

The object also retains advanced components used by
\code{\link[=compute_pgee_variances]{compute_pgee_variances()}}, including \code{X_list}, \code{y_list}, \code{mu_list},
\code{W_list}, \code{V_list}, \code{R_list}, \code{DT_list}, \code{r_list}, \code{H_list}, \code{I0}, and
\code{Delta}.
}
\description{
\code{pgee_fit()} fits a penalized generalized estimating equation (PGEE) model
for clustered binary outcomes using the low-level fitting routine currently
shipped in \code{pgeeVar}. The function is designed to retain the matrices and
residual objects needed by \code{\link[=compute_pgee_variances]{compute_pgee_variances()}}, so its return value is
deliberately more detailed than a typical user-facing model fit.
}
\details{
This is the low-level fitting engine underlying \code{\link[=pgee]{pgee()}}. Most end users
should call \code{\link[=pgee]{pgee()}} instead.

Rows with missing values in \code{y}, \code{x}, or \code{id} are removed before fitting.

When the optional \code{logistf} package is available, \code{pgee_fit()} uses Firth
logistic regression for starting values. Otherwise it falls back to
\code{glm(..., family = binomial())}, and finally to zero starts if needed.

The returned object is intentionally verbose because the package's variance
estimators operate on retained cluster-level matrices such as \code{H_list},
\code{V_list}, and \code{DT_list}.
}
\examples{
set.seed(1)
sim <- dgp_generate(
  N = 40,
  gamma = 0.5,
  n_i = 4,
  beta = c(-1.2, 0.6, 0.3),
  rho = 0.2
)

fit <- pgee_fit(
  y = sim$data$y,
  x = sim$data[c("X1", "obstime")],
  id = sim$data$id,
  corr_type = sim$corr_type
)

fit$converged
round(fit$beta, 3)
fit$alpha_hat

}
\references{
Firth D (1993). Bias reduction of maximum likelihood estimates.
\emph{Biometrika}, 80(1), 27-38.

Mondol MH and Rahman MS (2019). Bias-reduced and separation-proof GEE with
small or sparse longitudinal binary data. \emph{Statistics in Medicine}, 38(14),
2544-2560.
}
\seealso{
\code{\link[=pgee]{pgee()}}, \code{\link[=dgp_generate]{dgp_generate()}}, \code{\link[=compute_pgee_variances]{compute_pgee_variances()}},
\code{\link[=available_estimators]{available_estimators()}}
}
