% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{dgp_generate}
\alias{dgp_generate}
\title{Generate correlated binary longitudinal data}
\usage{
dgp_generate(N, gamma, n_i, beta, rho, corr_type = c("exchangeable", "ar1"))
}
\arguments{
\item{N}{Number of clusters.}

\item{gamma}{Proportion of clusters assigned the binary cluster-level
covariate \code{X1 = 1}.}

\item{n_i}{Cluster-size specification. Use a single integer for a balanced
design, or a length-2 vector \code{c(min, max)} to draw cluster sizes uniformly
between those limits.}

\item{beta}{Regression coefficients, including the intercept. Supported
lengths are 2, 3, and 4:
\itemize{
\item length 2: intercept and \code{X1}
\item length 3: intercept, \code{X1}, and \code{obstime}
\item length 4: intercept, \code{X1}, \code{obstime}, and a cluster-level continuous
covariate \code{X3}
}}

\item{rho}{True within-cluster correlation parameter used by the CLF
generator.}

\item{corr_type}{Correlation structure used for data generation. Must be
either \code{"exchangeable"} or \code{"ar1"}.}
}
\value{
A list with the following components:
\itemize{
\item \code{data}: observation-level data frame containing \code{id}, \code{y}, and the
generated covariates.
\item \code{covnames}: names of the non-intercept covariates to pass into
\code{\link[=pgee_fit]{pgee_fit()}}.
\item \code{N}, \code{N_min}, \code{N1}, \code{n_i}, \code{n_star}, \code{p}: basic design summaries.
\item \code{clf_ok}: logical flag indicating whether CLF generation succeeded for all
clusters.
\item \code{beta}, \code{rho}, \code{corr_type}: the generating parameters used for the
scenario.
}
}
\description{
\code{dgp_generate()} creates one simulated binary longitudinal dataset for the
PGEE settings studied in the \code{pgeeVar} manuscript. The function uses the
vendored Qaqish-style conditional linear family (CLF) machinery to generate
correlated binary outcomes under exchangeable or AR(1) dependence.
}
\details{
This is the low-level simulation engine underlying
\code{\link[=simulate_correlated_binary_data]{simulate_correlated_binary_data()}}. The returned object includes both the
observation-level dataset and the scenario metadata used to generate it, so
it can be passed directly into \code{\link[=pgee_fit]{pgee_fit()}} or reused in simulation scripts.

The returned dataset always includes an \code{id} column and a binary response
\code{y}. The covariate columns depend on the length of \code{beta}.

If the CLF construction fails for any cluster, \code{clf_ok} is set to \code{FALSE}
and the affected response values are returned as \code{NA}. The downstream
\code{\link[=pgee_fit]{pgee_fit()}} pipeline is still safe in that case because the fitting routine
removes incomplete rows with \code{complete.cases()} before estimation.
}
\examples{
set.seed(1)
sim <- dgp_generate(
  N = 12,
  gamma = 0.5,
  n_i = 4,
  beta = c(-1.2, 0.6, 0.3),
  rho = 0.2
)

sim$clf_ok
head(sim$data)
sim$covnames

}
\references{
Qaqish BF (2003). A family of multivariate binary distributions for
simulating correlated binary variables with specified marginal means and
correlations. \emph{Biometrika}, 90(2), 455-463.

The vendored helper layer also follows the archived \code{binarySimCLF} package
implementation of By and Qaqish (2009), CRAN package source.
}
\seealso{
\code{\link[=simulate_correlated_binary_data]{simulate_correlated_binary_data()}}, \code{\link[=pgee_fit]{pgee_fit()}},
\code{\link[=compute_pgee_variances]{compute_pgee_variances()}}
}
